import React from 'react';
import { storiesOf } from '@kadira/storybook';
import { MonthChart } from 'components';

storiesOf('MonthChart', module).add('default', () => (
  <MonthChart>default</MonthChart>
));
