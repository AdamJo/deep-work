import React from 'react';
import { storiesOf } from '@kadira/storybook';
import { YearChart } from 'components';

storiesOf('YearChart', module).add('default', () => (
  <YearChart>default</YearChart>
));
