import React from 'react';
import { storiesOf } from '@kadira/storybook';
import { CellChart } from 'components';

storiesOf('CellChart', module).add('default', () => (
  <CellChart>default</CellChart>
));
