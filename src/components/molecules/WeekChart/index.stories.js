import React from 'react';
import { storiesOf } from '@kadira/storybook';
import { WeekChart } from 'components';

storiesOf('WeekChart', module).add('default', () => (
  <WeekChart>default</WeekChart>
));
