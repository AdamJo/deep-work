import React from 'react';
import { storiesOf } from '@kadira/storybook';
import { MenuOptions } from 'components';

storiesOf('MenuOptions', module).add('default', () => (
  <MenuOptions>default</MenuOptions>
));
