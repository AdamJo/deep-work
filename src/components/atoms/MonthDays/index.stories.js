import React from 'react';
import { storiesOf } from '@kadira/storybook';
import { MonthDays } from 'components';

storiesOf('MonthDays', module).add('default', () => (
  <MonthDays>default</MonthDays>
));
