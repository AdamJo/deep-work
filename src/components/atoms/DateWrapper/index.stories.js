import React from 'react';
import { storiesOf } from '@kadira/storybook';
import { DateWrapper } from 'components';

storiesOf('DateWrapper', module).add('default', () => (
  <DateWrapper>default</DateWrapper>
));
