import React from 'react';
import { storiesOf } from '@kadira/storybook';
import { WeekDays } from 'components';

storiesOf('WeekDays', module).add('default', () => (
  <WeekDays>default</WeekDays>
));
