import React from 'react';
import { storiesOf } from '@kadira/storybook';
import { TotalHours } from 'components';

storiesOf('TotalHours', module).add('default', () => (
  <TotalHours>default</TotalHours>
));
