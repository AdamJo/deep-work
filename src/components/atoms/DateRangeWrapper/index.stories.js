import React from 'react';
import { storiesOf } from '@kadira/storybook';
import { DateRangeWrapper } from 'components';

storiesOf('DateRangeWrapper', module).add('default', () => (
  <DateRangeWrapper>default</DateRangeWrapper>
));
