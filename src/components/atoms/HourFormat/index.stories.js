import React from 'react';
import { storiesOf } from '@kadira/storybook';
import { HourFormat } from 'components';

storiesOf('HourFormat', module).add('default', () => (
  <HourFormat>default</HourFormat>
));
